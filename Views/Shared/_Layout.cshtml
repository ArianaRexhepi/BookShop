@{
    bool IsActiveLink(string controller, string action)
    {
        var routeData = ViewContext?.RouteData?.Values;

        var currentController = routeData?["Controller"]?.ToString();
        var currentAction = routeData?["Action"]?.ToString();

        return string.Equals(currentController, controller, StringComparison.OrdinalIgnoreCase) &&
        string.Equals(currentAction, action, StringComparison.OrdinalIgnoreCase);
    }

    bool IsActiveDropdownLink(string controller, string action)
    {
        var routeData = ViewContext?.RouteData?.Values;

        var currentController = routeData?["Controller"]?.ToString();
        var currentAction = routeData?["Action"]?.ToString();

        return string.Equals(currentController, controller, StringComparison.OrdinalIgnoreCase) &&
        string.Equals(currentAction, action, StringComparison.OrdinalIgnoreCase);
    }
}

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - Books</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/lib/fontawesome/css/all.css" asp-append-version="true" />
    @* <link rel="stylesheet" href="~/Books.styles.css" asp-append-version="true" /> *@
    <link rel="stylesheet" href="~/css/myCss.css" asp-append-version="true" />
    <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
    <script src="~/js/site.js"></script>
</head>

<body>
    <header>
        <nav class="navbar navbar-expand-sm navbar-toggleable-sm navbar-light bg-white border-bottom box-shadow mb-3">
            <div class="container-fluid">
                <a class="navbar-brand" asp-area="" asp-controller="Home" asp-action="Index">Books</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target=".navbar-collapse"
                    aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="navbar-collapse collapse d-sm-inline-flex justify-content-between">
                    <ul class="navbar-nav flex-grow-1">
                        <li class="nav-item">
                            <a class="nav-link text-dark @(IsActiveLink("Store", "Index")? "active" : "")" asp-area=""
                                asp-controller="Store" asp-action="Index">Books</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link text-dark @(IsActiveLink("BStore", "Bestseller")? "active" : "")" asp-area=""
                                asp-controller="BStore" asp-action="Index">Bestseller</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link text-dark @(IsActiveLink("MagStore", "Magazines")? "active" : "")" asp-area=""
                                asp-controller="MagStore" asp-action="Index">Magazines</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link text-dark @(IsActiveLink("AudStore", "Audiobooks")? "active" : "")" asp-area=""
                                asp-controller="AudStore" asp-action="Index">Audiobooks</a>
                        </li>




                        @if(User.IsInRole("Admin"))
                        {
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle text-dark @(IsActiveDropdownLink("Audiobooks", "Index") ||
                                                 IsActiveDropdownLink("Book", "Index") ||
                                                 IsActiveDropdownLink("Bestseller", "Index") ||
                                                 IsActiveDropdownLink("Magazines", "Index") ||
                                                 IsActiveDropdownLink("Admin", "Roles") ? "active" : "")" href="#"
                                id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true"
                                aria-expanded="false">
                                Lists
                            </a>
                            <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                                <a class="dropdown-item @(IsActiveLink("Audiobooks", "Index") ? "active" : "")"
                                    asp-area="" asp-controller="Audiobooks" asp-action="Index">Audiobooks List</a>
                                <a class="dropdown-item @(IsActiveLink("Book", "Index") ? "active" : "")" asp-area=""
                                    asp-controller="Book" asp-action="Index">Book List</a>
                                <a class="dropdown-item @(IsActiveLink("Bestseller", "Index") ? "active" : "")"
                                    asp-area="" asp-controller="Bestseller" asp-action="Index">Bestseller List</a>
                                <a class="dropdown-item @(IsActiveLink("Magazines", "Index") ? "active" : "")"
                                    asp-area="" asp-controller="Magazines" asp-action="Index">Magazines List</a>
                                <a class="dropdown-item @(IsActiveLink("Admin", "Roles") ? "active" : "")" asp-area=""
                                    asp-controller="Admin" asp-action="Roles">Roles</a>
                            </div>
                        </li>



                        }
                    </ul>
                    <div class="d-flex align-items-center">
                    <partial name="_LoginPartial" />
                    
                    @if (User.Identity.IsAuthenticated)
                    {
                        <a class="nav-link text-dark float-right" asp-area="" asp-controller="Cart" asp-action="Index"><i
                                class="fa-solid fa-cart-shopping"></i></a>
                    }
                    </div>
                </div>
            </div>
        </nav>
    </header>
    <div class="container">
        <main role="main" class="pb-3">
            @RenderBody()
        </main>
    </div>

    <footer class="border-top footer text-muted">
        <div class="container">
            &copy; 2024 - Books - <a asp-area="" asp-controller="Home" asp-action="Privacy">Privacy</a>
        </div>
    </footer>
    <script src="https://code.jquery.com/jquery-3.3.1.slim.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.7/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js"></script>
    @await RenderSectionAsync("Scripts", required: false)
</body>

</html>
